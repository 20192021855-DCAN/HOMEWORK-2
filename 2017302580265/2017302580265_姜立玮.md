## 第二次作业
**P3**
```
a. 电路交换网。因为应用将以稳定的速率传输数据，并且连续运行相当长一段时间，因此可以为其保留带宽。
b. 不需要。因为传输速率总和小于链路容量。
```

**P6**
```
a. d(prop)=m/s
b. 传输时间d(trans)=L/R
c. 端到端时延：d(nodal)=m/s+L/R
d. 该分组的最后一个比特刚刚离开A主机
e. 该分组的第一个比特在链路中且没有到达B
f. 该分组的第一个比特已到达B主机
g. m=LS/R=523.16(km)
```

**P7**
```
考虑数据包中的第一位。在传输此位之前，必须生成数据包。
主机A产生56字节的分组需要 56 * 8b / 64kbps = 7ms
传输时延d(trans) = L/R = 56 * 8b / 2Mbps = 0.224ms
总时间 = 7ms + 10ms + 0.224ms = 17.224ms
```




